import type { OrbitasErrorCode } from '@/features/errors/catalog';
export const mapJoinValidationErrorToCode = (error: string): OrbitasErrorCode => error==='room_not_found'?'ROOM_NOT_FOUND':error==='room_full'?'ROOM_FULL':(error==='room_invalid_state'||error==='room_closed_or_expired'||error==='room_in_game')?'ROOM_NOT_JOINABLE':'UNKNOWN';
export const mapStartMatchErrorToCode = (code: string): OrbitasErrorCode => code==='NOT_HOST'?'NOT_HOST':code==='INVALID_PLAYER_COUNT'?'MIN_PLAYERS_NOT_MET':code==='PROMPTS_UNAVAILABLE'?'PROMPTS_UNAVAILABLE':(code==='ROOM_NOT_IN_LOBBY'||code==='MATCH_ALREADY_STARTED'||code==='MODE_NOT_SELECTED')?'ROOM_NOT_JOINABLE':'UNKNOWN';
export const mapRoundErrorToCode = (code: string): OrbitasErrorCode => code==='NOT_HOST'?'NOT_HOST':(code.includes('ROUND_NOT_')||code==='INVALID_ROUND_STATE'||code==='ROUND_NOT_FOUND')?'ROUND_STATE_INVALID':(code.includes('PARTICIPANT_NOT_IN_ROOM')||code.includes('INVALID_')||code.includes('EMPTY_TEXT')||code.includes('TEXT_TOO_LONG')||code.includes('TARGET_NOT_IN_MATCH'))?'SUBMISSION_REJECTED':'UNKNOWN';
